name: Release

on:
  push:
    tags:
      - "v*"
  pull_request:

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  audit:
    name: Audit
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Audit
        run: bun audit

  publish:
    name: Publish
    runs-on: ubuntu-latest
    needs: [audit]
    permissions:
      contents: read
      id-token: write
      packages: write
    strategy:
      fail-fast: false
      matrix:
        registry: [npm, github]
        include:
          - registry: npm
            registry-url: https://registry.npmjs.org
          - registry: github
            registry-url: https://npm.pkg.github.com
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          registries: ${{ matrix.registry == 'github' && format('@kjanat:{0}|$GITHUB_TOKEN', matrix.registry-url) || '' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup publishing context
        id: context
        uses: ./.github/actions/setup-context
        with:
          package: "@kjanat/prettier-config"
          registry-url: ${{ matrix.registry-url }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        if: steps.context.outputs.should-publish == 'true'
        run: bun install --frozen-lockfile

      - name: Setup Node (npm OIDC trusted publishing)
        if: matrix.registry == 'npm' && steps.context.outputs.should-publish == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: latest
          registry-url: https://registry.npmjs.org

      - name: Publish to npm registry${{ github.event_name != 'push' && ' (dry run)' || '' }}
        if: matrix.registry == 'npm' && steps.context.outputs.should-publish == 'true'
        env:
          DRY_RUN_FLAG: ${{ github.event_name != 'push' && '--dry-run' || '' }}
        run: npm publish --access public $DRY_RUN_FLAG

      - name: Publish to GitHub Packages registry${{ github.event_name != 'push' && ' (dry run)' || '' }}
        if: matrix.registry == 'github' && steps.context.outputs.should-publish == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REGISTRY_URL: ${{ matrix.registry-url }}
          DRY_RUN_FLAG: ${{ github.event_name != 'push' && '--dry-run' || '' }}
        run: bun publish --access public --registry "$REGISTRY_URL" $DRY_RUN_FLAG

      - name: Publishing skipped
        if: steps.context.outputs.should-publish != 'true'
        env:
          IS_TAG: ${{ steps.context.outputs.is-tag }}
          TAG_VERSION: ${{ steps.context.outputs.tag-version }}
          REGISTRY: ${{ matrix.registry }}
        run: |
          if [[ "$IS_TAG" == "true" ]]; then
            echo "⏭️ Version $TAG_VERSION already published to $REGISTRY"
          else
            echo "ℹ️ Not a version tag - skipping $REGISTRY publish"
          fi
